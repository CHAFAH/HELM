apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hilltop.fullname" . }}-test-connection"
  labels:
    {{- include "hilltop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hilltop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
